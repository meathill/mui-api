DeepSeek a publié la version stable de son modèle phare — **DeepSeek-V4-Pro-0813** (`deepseek-v4-pro`) — complétant la famille V4 lancée avec l'aperçu d'avril. La version officielle conserve l'architecture **MoE de 1,6 billion de paramètres au total / 49 milliards d'actifs** et la fenêtre de contexte de 1M signature de la gamme, avec une sortie maximale de 384K tokens.

`deepseek-v4-pro` est désormais disponible sur MuiRouter. Avec une seule clé API et un simple changement de nom de modèle, vous pouvez l'appeler via l'endpoint compatible OpenAI standard (`/v1/chat/completions`).

## Points forts

| Fonctionnalité | DeepSeek V4 Pro |
| --- | --- |
| **Architecture** | MoE 1,6T paramètres au total, 49B actifs par token |
| **Fenêtre de contexte** | 1 048 576 tokens (1M) |
| **Sortie maximale** | jusqu'à 384K tokens |
| **Mode de réflexion** | non-réflexion / réflexion (par défaut) |
| **Fonctionnalités** | JSON Output, appels d'outils, API Responses, API Anthropic, préfixe de chat, FIM (non-réflexion uniquement) |

## Tarifs officiels et facturation MuiRouter

Tarifs officiels de l'API DeepSeek :

| Type de token | Prix (par 1M tokens) |
| --- | ---: |
| **Entrée (Échec de cache)** | **0,435 $** |
| **Entrée (Succès de cache)** | **0,003625 $** |
| **Sortie** | **0,87 $** |

Sur MuiRouter, `deepseek-v4-pro` applique notre markup standard de 1,2 : **0,522 $ / 0,00435 $ / 1,044 $ par million de tokens** (entrée, entrée en cache, sortie). À noter : DeepSeek a officiellement annoncé une hausse globale des prix dans un avenir proche — une bonne raison de profiter des tarifs actuels.

## Conclusion

Le modèle phare à poids ouverts de la famille DeepSeek V4 est désormais disponible en version stable : contexte 1M, modes réflexion et non-réflexion, et des coûts de prompt caching très agressifs pour une fraction du prix des modèles fermés comparables. Essayez-le dès aujourd'hui sur le Playground MuiRouter, dans votre agent de terminal ou via l'API compatible OpenAI — facturation transparente à l'usage, sans abonnement.
