{
  "name": "@muirouter/cli",
  "version": "1.0.0",
  "description": "MuiRouter 项目接入、配置与升级工具",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "muirouter": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node scripts/build.ts",
    "prepack": "pnpm run build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3",
    "@types/node": "^24.7.2",
    "vitest": "^4.1.1"
  }
}
